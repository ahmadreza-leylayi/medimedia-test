# راهنمای شروع سریع

این راهنما برای کسانی که تازه وارد پروژه شده‌اند نوشته شده است.

## 🎯 هدف پروژه

این پروژه یک **پنل مدیریت داشبورد** برای سیستم مدی‌مدیا است که شامل:
- مدیریت کاربران
- مدیریت نوبت‌ها
- نمایش آمار و نمودارها
- جستجو و فیلتر

## 🚀 شروع کار

### 1. نصب Dependencies

```bash
npm install
```

### 2. اجرای Development Server

```bash
npm run dev
```

پروژه در `http://localhost:3000` اجرا می‌شود.

### 3. دسترسی به صفحات

- **صفحه اصلی**: `http://localhost:3000`
- **داشبورد**: `http://localhost:3000/dashboard`
- **صفحه Home**: `http://localhost:3000/home`

## 📚 یادگیری ساختار

### گام 1: مطالعه مستندات

1. **README.md**: راهنمای کلی پروژه
2. **ARCHITECTURE.md**: معماری کامل پروژه (⚠️ مهم!)
3. **CONTRIBUTING.md**: راهنمای مشارکت

### گام 2: بررسی ساختار پوشه‌ها

```
src/
├── app/              # صفحات Next.js
├── components/       # کامپوننت‌های React
├── redux/           # State Management
└── types/           # TypeScript Types
```

### گام 3: شروع با یک کامپوننت ساده

**مثال**: بررسی `Card` component

```typescript
// src/components/dashboard/ui/Card.tsx
```

این کامپوننت:
- یک کامپوننت UI پایه است
- قابل استفاده مجدد است
- TypeScript دارد
- RTL را پشتیبانی می‌کند

## 🔍 مفاهیم کلیدی

### 1. Redux State Management

```typescript
// گرفتن state
const { employees } = useAppSelector((state) => state.dashboard);

// تغییر state
dispatch(addEmployee(newEmployee));
```

### 2. Component Structure

```typescript
'use client'; // برای hooks

interface Props {
  title: string;
}

export const MyComponent: React.FC<Props> = ({ title }) => {
  return <div>{title}</div>;
};
```

### 3. RTL Support

```typescript
<div dir="rtl" className="text-right">
  متن فارسی
</div>
```

## 🛠️ ابزارهای مورد نیاز

- **Node.js**: v18 یا بالاتر
- **npm** یا **yarn**
- **VS Code** (توصیه می‌شود)
- **Extension های VS Code**:
  - ESLint
  - Prettier
  - TypeScript

## 📖 منابع یادگیری

### برای مبتدیان:

1. **React**: [React Docs](https://react.dev/learn)
2. **Next.js**: [Next.js Tutorial](https://nextjs.org/learn)
3. **TypeScript**: [TypeScript Handbook](https://www.typescriptlang.org/docs/)

### برای پیشرفته:

1. **Redux Toolkit**: [Redux Toolkit Docs](https://redux-toolkit.js.org)
2. **Next.js App Router**: [App Router Docs](https://nextjs.org/docs/app)

## ❓ سوالات متداول

### Q: چطور یک کامپوننت جدید اضافه کنم؟

**A**: به [CONTRIBUTING.md](./CONTRIBUTING.md) مراجعه کنید.

### Q: چطور state را مدیریت کنم؟

**A**: از Redux Toolkit استفاده کنید. به [src/redux/README.md](./src/redux/README.md) مراجعه کنید.

### Q: چطور یک صفحه جدید اضافه کنم؟

**A**: در `src/app/` یک پوشه جدید ایجاد کنید و `page.tsx` اضافه کنید.

### Q: چرا کد من کار نمی‌کند؟

**A**: 
1. TypeScript errors را بررسی کنید
2. Console errors را چک کنید
3. از کامنت‌ها استفاده کنید
4. از تیم بپرسید

## 🎓 تمرین‌های پیشنهادی

### تمرین 1: بررسی یک Panel

1. `AppointmentPanel.tsx` را باز کنید
2. ساختار آن را بررسی کنید
3. Props را ببینید
4. State management را بررسی کنید

### تمرین 2: اضافه کردن یک Feature کوچک

1. یک دکمه ساده اضافه کنید
2. با کلیک، یک console.log نمایش دهید
3. State را تغییر دهید

### تمرین 3: درک Redux Flow

1. یک action را در `dashboardSlice.ts` پیدا کنید
2. ببینید چطور در component استفاده می‌شود
3. State را تغییر دهید و نتیجه را ببینید

## 💡 نکات مهم

1. **همیشه TypeScript errors را بررسی کنید**
2. **از کامنت‌ها استفاده کنید**
3. **RTL را رعایت کنید**
4. **کد را تمیز نگه دارید**
5. **از تیم بپرسید اگر سوالی دارید**

---

**موفق باشید! 🚀**

اگر سوالی دارید، از تیم بپرسید یا در GitHub Issues ایجاد کنید.

