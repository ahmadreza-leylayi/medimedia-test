# Dashboard Panels

این پوشه شامل تمام پنل‌های داشبورد است. هر پنل یک بخش مستقل از داشبورد است.

## 📋 پنل‌های موجود

### 1. AppointmentPanel
**مسئولیت**: مدیریت نوبت‌ها و تاریخ/زمان

**ویژگی‌ها**:
- نمایش تاریخ و زمان فعلی
- لیست نوبت‌ها
- افزودن نوبت جدید
- تغییر وضعیت نوبت (انجام شده، لغو شده)
- حذف نوبت

**Props**:
```typescript
interface AppointmentPanelProps {
  currentTime: string;
  currentDate: string;
  selectedDate: Date;
  appointments: Appointment[];
  // ...
}
```

---

### 2. QuickActionsPanel
**مسئولیت**: عملیات سریع (جستجو + منو)

**ویژگی‌ها**:
- جستجوی پزشک
- منوی اصلی
- دکمه‌های دسترسی سریع

**کامپوننت‌های داخلی**:
- `SearchPanel`: جستجوی پزشک
- `MenuPanel`: منوی اصلی

---

### 3. StatisticsChartPanel
**مسئولیت**: نمایش نمودار آمار

**ویژگی‌ها**:
- نمودار ستونی
- فیلتر بر اساس دوره (روز، هفته، ماه، سال)
- Legend با رنگ‌های مختلف

**Props**:
```typescript
interface StatisticsChartPanelProps {
  activePeriod: 'day' | 'week' | 'month' | 'year';
  onPeriodChange: (period: ...) => void;
}
```

---

### 4. SearchPanel
**مسئولیت**: جستجوی پزشک برای نسخه نویسی

**ویژگی‌ها**:
- جستجو در لیست پزشکان
- نمایش نتایج در dropdown
- انتخاب پزشک
- نمایش پزشک انتخاب شده

---

### 5. MenuPanel
**مسئولیت**: منوی اصلی داشبورد

**ویژگی‌ها**:
- دکمه‌های دسترسی سریع
- لیست منوها با زیرمنو
- دکمه مدیریت منو

---

### 6. AddAppointmentForm
**مسئولیت**: فرم افزودن نوبت جدید

**ویژگی‌ها**:
- ورود نام بیمار
- ورود ساعت نوبت (با فونت فارسی)
- Validation
- ذخیره نوبت

---

## 🔧 نحوه استفاده

```typescript
import { AppointmentPanel } from '@/components/dashboard/panels/AppointmentPanel';

<AppointmentPanel
  currentTime={currentTime}
  appointments={appointments}
  onAddAppointmentClick={handleAdd}
  // ...
/>
```

## 📝 افزودن پنل جدید

1. فایل جدید در `panels/` ایجاد کنید
2. Component را بنویسید
3. به `panels/index.ts` export اضافه کنید
4. در `DashboardMainPage` استفاده کنید

