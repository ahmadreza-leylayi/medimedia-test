# Utility Functions

این پوشه شامل توابع کمکی و utility functions است.

## 📝 استفاده

```typescript
import { normalizeTime, validateTime, formatSelectedDate } from '@/lib/utils';
```

## 🔧 توابع موجود

### `utils/date.ts`
توابع مربوط به تاریخ و زمان:

- `normalizeTime`: تبدیل هر فرمت زمانی به فرمت فارسی استاندارد (HH:MM)
- `validateTime`: بررسی صحت فرمت زمان
- `parsePersianTime`: تبدیل زمان فارسی به دقیقه برای مقایسه
- `getCurrentTimeInMinutes`: دریافت زمان فعلی به دقیقه
- `isTimePassed`: بررسی اینکه آیا زمان نوبت گذشته است
- `formatSelectedDate`: فرمت تاریخ برای نمایش (تاریخ کامل با روز هفته)
- `formatMonthYear`: فرمت ماه و سال برای نمایش
- `formatTime`: فرمت زمان برای نمایش
- `isDateInPast`: بررسی اینکه آیا تاریخ در گذشته است
- `isDateInFuture`: بررسی اینکه آیا تاریخ در آینده است
- `isToday`: بررسی اینکه آیا تاریخ امروز است
- `getDateString`: دریافت رشته تاریخ به فرمت YYYY-MM-DD

## 📌 ساختار

```
lib/
├── utils/
│   ├── date.ts      # توابع تاریخ و زمان
│   └── index.ts     # Export مرکزی
└── README.md
```

## 🔧 افزودن Utility جدید

هنگام افزودن utility جدید:
1. فایل را در `utils/` ایجاد کنید
2. آن را در `utils/index.ts` export کنید
3. مستندات را در این فایل به‌روزرسانی کنید

